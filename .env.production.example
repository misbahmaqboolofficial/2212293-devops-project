# Copy to .env.production on EC2 (never commit real credentials)
# Used by the CD pipeline: cp .env.production .env

DB_USER=devops_user
DB_PASSWORD=change_this_strong_production_password
DB_NAME=devops_db

